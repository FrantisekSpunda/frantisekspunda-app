import { getListWidths, toArray, unique } from './utils.array';
import { cn, cut, slug, newShade, humanFileSize } from './utils.string';
import { getRoute } from './utils.route';
import { newId } from './utils.newid';
import { template } from './utiles.template';
import { mapObject } from './utils.object';

export {
  cn,
  cut,
  slug,
  newShade,
  unique,
  getRoute,
  newId,
  toArray,
  getListWidths,
  humanFileSize,
  template,
  mapObject
}